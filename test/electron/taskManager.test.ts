import { describe, it } from 'vitest';

describe('taskManager unit', () => {
    describe('killProcess', () => {
        it.todo('should successfully kill a process', async () => {
        });

        it.todo('should throw an error if unable to kill a process', async () => {
        });
    });

    describe('getProcessDirectory', () => {
        it.todo('should successfully get the process directory', async () => {
        });

        it.todo('should throw an error if unable to find the process directory', async () => {
        });
    });

    describe('openDirectoryInFinder', () => {
        it.todo('should successfully open a directory in Finder', async () => {
        });

        it.todo('should throw an error if the directory does not exist', async () => {
        });

        it.todo('should throw an error if unable to open the directory in Finder', async () => {
        });
    });
});